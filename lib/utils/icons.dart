/// In  this code  bas was  written all of the  icons of project

import 'package:flutter/material.dart';

class MainIcons{

  static Icon phone = const Icon(Icons.phone);
  static Icon password = const Icon(Icons.lock);
  static Icon eye = const Icon(Icons.visibility);
  static Icon eyeClose = const Icon(Icons.visibility_off);

}